
1. 导入账单文件格式不正确的效果有闪烁，需要修复
2. 导入的前端应该也做文件类型的检测，直接前端给拦截
3. 列表显示已退款（xxx）的状态异常，这种情况只有微信账单才有

4. 账单导入对于主键重复的检查结果要直接显示
5. 导入数据变成异步处理



聚合账单拆分
- 聚合账单拆分功能，可以将微信账单拆分成多个账单，每个账单是一个单独的消费，合起来就是一个大的组合支付
- 聚合账单 表格右边设置拆分按钮 点击弹出差费框
- 自动填充其他信息，让用户填充价格，商品等信息，点击新增一行拆分第二项...
- 提交校验金额是否一致


需要两张表   
- 账单表 
- 记账表
- 两张表加字段，是否checked

账单check 提供时间可选


前端不展示订单的具体时间 只展示日期

家庭单位记账，消费可能被多人重复记录

加一列 ： 消费者  这笔账单是谁消费的


数据字典应该只能看到自己的，除非你是管理员


支持菜单动态显示，用户可选


账单除了备注还有明细 明细是商品信息 备注是其他信息
记账的标题改成账单周期
消费人是多选，可以选择多个消费者

深度支出和二级菜单解绑，可以不选
一级菜单必选，二级菜单选填
账单周期也可以不选，默认是当月
账单周期也解绑
解绑但是都给默认值
记账 增加账户（必选项）和消费方式（非必选）

新增预算 预算以一级菜单为单位


汇总页面的表格可以点击，显示详细信息

